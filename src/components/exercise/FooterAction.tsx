import { Button } from "konsta/react";

const FooterAction = () => {
  return (
    <Button large className="left-0 bottom-0 fixed rounded-b-none">
      Davom etish
    </Button>
  );
};

export default FooterAction;
