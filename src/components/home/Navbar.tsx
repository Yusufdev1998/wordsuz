import Rank from "./Rank";
import Search from "./Search";

const Navbar = () => {
  return (
    <nav>
      <Search />
      <Rank />
      {/* Sections */}
    </nav>
  );
};

export default Navbar;
